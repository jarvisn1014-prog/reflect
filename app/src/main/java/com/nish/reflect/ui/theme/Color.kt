package com.nish.reflect.ui.theme

import androidx.compose.ui.graphics.Color

// Apple-inspired minimalist palette + warm accent for emotional contexts

// Light theme
val LightBackground = Color(0xFFF2F2F7)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFE5E5EA)
val LightOnSurface = Color(0xFF1C1C1E)
val LightOnSurfaceVariant = Color(0xFF6B6B70)
val LightHairline = Color(0xFFC6C6C8)

// Dark theme
val DarkBackground = Color(0xFF000000)
val DarkSurface = Color(0xFF1C1C1E)
val DarkSurfaceVariant = Color(0xFF2C2C2E)
val DarkOnSurface = Color(0xFFFFFFFF)
val DarkOnSurfaceVariant = Color(0xFF8E8E93)
val DarkHairline = Color(0xFF38383A)

// Single accent — Indigo (Apple-style)
val Accent = Color(0xFF5E5CE6)
val AccentVariant = Color(0xFF7D7AFF)

// Warm accent — shifted 5° toward purple for emotional contexts
val AccentWarm = Color(0xFF6E5CE6)

// Muted slate for low-mood chart bars
val EmotionSlate = Color(0xFF6B6B70)

// Semantic
val Success = Color(0xFF34C759)
val Warning = Color(0xFFFF9500)
val Error = Color(0xFFFF3B30)